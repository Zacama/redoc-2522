events {}


http {
    server {
        listen 80;
        location / {
            root   /usr/share/nginx/html;
            autoindex on;
            autoindex_localtime on;
            charset utf-8;
        }
    }
}
